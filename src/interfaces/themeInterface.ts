export interface Theme {
    name: string;
    primary: string;
    secondary: string;
    colors: {
        success: string;
        danger: string;
        warning: string;
        info: string;
    }
}